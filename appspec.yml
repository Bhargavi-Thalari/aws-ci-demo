version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/sample-java-app

hooks:
  ApplicationStop:
    - location: stop.sh
      timeout: 300
      runas: ubuntu

  ApplicationStart:
    - location: start.sh
      timeout: 300
      runas: ubuntu
